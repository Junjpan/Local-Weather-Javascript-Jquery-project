This project is for creating a webpage to display the Local weather, the tools I used are JavaScript, jQuery, Boostrap ,CSS3 and HTML5. 

This project is also includes retrieving data from the JSON object. 
